[Unit]
Description=server
Requires=After=mysql.service

[Service]
WorkingDirectory=/opt/app/
ExecStart=/usr/bin/node /opt/app/server.js > /opt/app/log.txt
Restart=always

[Install]
WantedBy=multi-user.target
